



// We have implemented this function for you 
export function printEveryItem(items) {
    items.forEach((v) => {
        console.log(v); 
    }); 
}


// Hint: use .forEach
export function printEveryItemPrefixed(prefix, items) {

}

// Hint: use .forEach
export function printEverySecondItem(items) {

}



// If it's the first item prefix with 'head:'
// If it's the last item, prefix with 'tail:'
// Otherwise prefix with 'value:'  

//Hint: use .forEach
export function printWithSpecialLabels(items) {

}


// We have implemented this function for you
export function timesValuesBy10(items) {
    return items.map(v => v * 10); 
}

// Convert convert numbers 1,2,3 to objects like {value: 1}, {value: 2} ... 
// Hint: use .map
export function createValuesObjectsFromNumbers(items) {

}

// Like above, but if it is the first item, it has isHead: true, if it is the last item it has isTail: true 
// Hint: use .map
export function createValueObjectsWithSpecialProperties(items) {

}




export function printEveryPermutation(listA, listB) {
    
}