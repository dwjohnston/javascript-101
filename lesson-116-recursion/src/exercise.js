

// implement the function such that the tests pass.
export function fibonacci(n) {

    if (n <2) {
        return 1; 
    }

    else {

    }

    // A note for the more astute observer - the solution needed for this function signature will not be especially efficient!
    // It requires making twice the number of function calls than should be necessary.
}