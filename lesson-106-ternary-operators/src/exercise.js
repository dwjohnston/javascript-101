

/**
 * Vector will be a number from -100 to 100. If the number is positive return "north", otherwise return "south"
 * There is a gentle breeze blowing north, so if the number is 0, return "north"
 */
export function getDirection(vector) {
    
}


/** 
 * This character only moves north an south. 
 * Given a direction "north" or "south" return their new position
 * Position is an object of the shape: {x: 0, y: 0} for example
 * 
 */ 
export function moveCharacter(position, direction) {

    
    
}