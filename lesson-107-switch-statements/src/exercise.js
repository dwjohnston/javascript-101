
// This function is complete, you do not need to use it. 
export function addVector(position, vector) {
    return {
        x: position.x + vector.x, 
        y: position.y + vector.y, 
    }
}



/**
 *  Possible directions are "north", "south", "east", "west", and "halt"
 * 
    Hint, use the addVector method!

 */
export function moveCharacter(position, direction) {

    
    
}